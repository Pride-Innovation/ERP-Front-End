export const availability = {
    available: "available",
    on_leave: "on leave"
}

export const crudStates: {
    create: string;
    update: string;
    read: string;
    delete: string
} = {
    create: "create",
    update: "update",
    read: "read",
    delete: "delete"
}