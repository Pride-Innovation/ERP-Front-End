export const availability = {
    available: "available",
    on_leave: "on leave"
}

export const crudStates: {
    create: string;
    update: string;
    read: string;
    delete: string;
    dispose: string;
} = {
    create: "create",
    update: "update",
    read: "read",
    delete: "delete",
    dispose: "dispose"
}

export const requestStatus: {
    pending: string;
    approved: string;
    rejected: string;
} = {
    pending: "pending",
    approved: "approved",
    rejected: "rejected"
}

export const assetStatus: {
    use: string;
    store: string;
    repair: string;
    disposed: string;
    active: string;
} = {
    use: "use",
    store: "store",
    repair: "repair",
    disposed: "disposed",
    active: 'active'
}